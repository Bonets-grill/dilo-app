// Updated by scripts/stamp-build.mjs on every build. The exported constant
// appears as a tiny version badge in the DM header and the chat footer so we
// can tell from screenshots which build the user's device actually loaded.
export const BUILD_ID = "2026-04-16_1057";
